{
  "files": [ // 编译器需要编译的单个文件的列表
    "src/components/demo/Hello.tsx"
  ],
  "include": [ // 编译器需要编译的文件或者目录 src/* 表示 src底下一级目录的文件
    
  ],
  "exclude": [ // 编译器需要排除的文件或者目录 默认会排除node_modules
    "src/libs"
  ],
  "references": [] // 依赖
}
